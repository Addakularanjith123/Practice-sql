# Write your MySQL query statement below
DELETE e1
FROM Person e1
JOIN Person e2
  ON e1.email = e2.email
 AND e1.id > e2.id;


#Even the ID's are also duplicate
-- DELETE FROM Person
-- WHERE id IN (
--     SELECT id
--     FROM (
--         SELECT 
--             id,
--             ROW_NUMBER() OVER (
--                 PARTITION BY email
--                 ORDER BY id
--             ) AS rn
--         FROM Person
--     ) t
--     WHERE rn > 1
-- );
