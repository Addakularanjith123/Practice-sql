-- select * from employee;
select max(salary) as salary
from employee
where salary < (select max(salary) from employee)

-- -- select salary #distinct is not required because already group by removes the duplicates
-- -- from employee
-- -- group by salary
-- -- order by salary desc
-- -- limit 1
-- -- offset 1

-- select salary
-- from (
--     select salary,dense_rank() over(order by salary desc) as dnk
--     from employee
--     )t
-- where dnk=2

