-- -- -- select * from employee;

select department,first_name,salary, avg(salary) over(partition by department) as avg_sal 
from employee;

-- with cte as(
--     select department,avg(salary) as avg_salary
--     from employee
--     group by department
--     )
-- select c.department,e.first_name,e.salary,c.avg_salary
-- from cte c
-- join employee e on e.department=c.department
-- order by department


-- -- select department, first_name, salary, avg(salary) over (partition by department)
-- -- from employee
-- -- group by department, first_name, salary





