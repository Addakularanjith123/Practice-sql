select trackname,count(*) as times_top1
from spotify_worldwide_daily_song_ranking
where position='1'
group by trackname
order by times_top1 desc

-- with cte as (
--     select trackname,count(*) over(partition by trackname) as times_top1
--     from spotify_worldwide_daily_song_ranking
--     where position='1'
--     )
-- select * 
-- from cte
-- group by trackname
-- order by times_top1 desc

